import React from 'react';
import {withRouter} from 'react-router-dom';

class SetReminderPage extends React.Component {
  render() {
    return (
      <div>set reminder page</div>
    );
  }
}

export default withRouter(SetReminderPage);
